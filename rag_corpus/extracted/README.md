# Extracted source records

This folder contains unified source records produced from raw datasets. These are still source records, not final RAG rules.

Use LLM normalization to create `rag_corpus/processed/*.jsonl`.
